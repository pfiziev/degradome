The HeLa reads are reverse-complemented. Their reverse complement must be used in the analysis instead. trim_reads.py converts them correctly.
peakData.10.hg19.U87.both.unique.lax are the small RNA peaks from Chris.

